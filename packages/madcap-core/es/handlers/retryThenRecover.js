export function retryThenRecover(error /*, retry, retryTimes, recover*/) {
    console.log(error);
    // if (error.retry && error.retriedCount) {
    // }
}
//# sourceMappingURL=retryThenRecover.js.map